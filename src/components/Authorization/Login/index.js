import React, { Component } from 'react';import style from './Login.module.sass'import { connect } from 'react-redux';class LoginBox extends Component {    constructor(props) {        super(props);        this.state = {            accounts: this.props.accounts,            isPasswordValid: false,            isUsernameValid: false,            stylePassword: style.loginInput,            styleUsername: style.loginInput,        };    }    submitLogin = () => {        const {accounts} = this.props;        const{password, username} = this.refs;        if (!(this.state.isPasswordValid && this.state.isUsernameValid)) {            return        }        const passwordValue = password.value;        const loginValue = username.value.toUpperCase()        const isValid = accounts.some(account => account.login.toUpperCase() === loginValue            && account.password === passwordValue)        console.log(isValid)    }    handlePasswordChange = (e) => {        if (e.target.value.length === 0) {            this.setState({ stylePassword: `${style.loginInput} ${style.invalid}`, isPasswordValid: false });            return        }        this.setState({ stylePassword: `${style.loginInput}`, isPasswordValid: true });    }    handleUsernameChange = (e) => {        if (e.target.value.length === 0) {            this.setState({                styleUsername: `${style.loginInput} ${style.invalid}`,                isUsernameValid: false            });            return        }        this.setState({            styleUsername: `${style.loginInput}`,            isUsernameValid: true        });    }    render() {        return (            <div className={style.mainContainer}>                <div className={style.innerContainer}>                    <div className={style.box}>                        <div className={style.inputGroup}>                            <span className={style.user}></span>                            <input type="text"                                name="username"                                className={this.state.styleUsername}                                placeholder="Username"                                ref="username"                                onChange={this.handleUsernameChange} />                        </div>                        <div className={style.inputGroup}>                            <span className={style.lock}></span>                            <input type="password"                                name="password"                                className={this.state.stylePassword}                                placeholder="Password"                                ref="password"                                onChange={this.handlePasswordChange}                            />                        </div>                        <button className={style.loginBtn} type="button" onClick={this.submitLogin}>Login</button>                    </div>                </div>            </div>        );    }}const mapStateToProps = (storeState) => ({    accounts: storeState.accounts})export default connect(mapStateToProps)(LoginBox);